{
  "name": "flatemate-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && chmod +x node_modules/.bin/prisma && npx prisma generate",
    "start": "cd backend && node server.js"
  }
}
